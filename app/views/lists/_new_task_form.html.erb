<%= simple_form_for @task, remote: true do |f|  %>
  <% f.hidden_field :list_id, :value => @current_list.id %>
  <%= f.input :content, label: false, placeholder: "Add a new list item...", input_html: { id: 'new_task_input', autocomplete: "off" } %>
<% end %>